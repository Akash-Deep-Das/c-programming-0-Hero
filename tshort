#include<iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    while(n--)
    {
        int a[n];
        for(int i=0; i<n;i++)
        cin>>a[i];
		}
        for(int i=0;i<n;i++){
        if(a[i]<a[i++])
cout<<a[i]<<endl;
else{
int temp=a[i++];
a[i++]=a[i];
a[i]=temp;
cout<<a[i]<<endl;}}
    return 0;
}
